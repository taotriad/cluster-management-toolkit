---
kind: "LXCMachine"
api_family: "infrastructure.cluster.x-k8s.io"
default_command: "lxcmachines"
command:
  - "lxcmachs"
  - "lxcmach"
listview:
  name: "LXC Machines"
  group: "Cluster Configuration"
  field_indexes:
    Wide:
      fields: ["namespace", "name", "controller", "cluster", "flavor",
               "instance_type", "condition_ready", "age"]
      sortcolumn: "namespace"
    Normal:
      fields: ["namespace", "name", "cluster", "flavor",
               "condition_ready", "age"]
      sortcolumn: "namespace"
  fields:
    cluster:
      header: "Cluster:"
      path: "metadata#labels#cluster.x-k8s.io/cluster-name"
      default: "<unset>"
      type: "str"
    {%- for line in field_condition_ready.split("\n") %}
    {{ line }}
    {%- endfor %}
    {%- for line in field_controller.split("\n") %}
    {{ line }}
    {%- endfor %}
    flavor:
      header: "Flavor:"
      path: "spec#flavor"
      default: "<unset>"
      type: "str"
    instance_type:
      header: "Instance Type:"
      path: "spec#instanceType"
      default: "<unset>"
      type: "str"
infoview:
  name: "LXC Machine Info"
  infopad:
    row_indexes:
      Normal:
        fields: ["controller", "cluster", "flavor",
                 "instance_type", "profiles", "condition_ready"]
    rows:
      {%- for line in row_condition_ready.split("\n") %}
      {{ line }}
      {%- endfor %}
      {%- for line in row_controller.split("\n") %}
      {{ line }}
      {%- endfor %}
      cluster:
        header: [["Cl", ["main", "infoheader"]],
                 ["u", ["main", "infoheader_shortcut"]],
                 ["ster:", ["main", "infoheader"]]]
        path: "metadata#labels#cluster.x-k8s.io/cluster-name"
        default: "<unset>"
        type: "str"
      flavor:
        header: [["Flavor:", ["main", "infoheader"]]]
        path: "spec#flavor"
        default: "<unset>"
        type: "str"
      instance_type:
        header: [["Instance Type:", ["main", "infoheader"]]]
        path: "spec#instanceType"
        default: "<unset>"
        type: "str"
      profiles:
        header: [["Profiles:", ["main", "infoheader"]]]
        path: "spec#profiles"
        type: "raw"
        formatter: "list"
  shortcuts:
    {%- for line in shortcut_controller.split("\n") %}
    {{ line }}
    {%- endfor %}
    "Open Cluster":
      key: "u"
      read_only: true
      helptext: "Open info page for Cluster"
      action: "call"
      action_call: "resourceinfodispatch_with_lookup"
      action_args:
        _pass_obj: true
        kind: "Cluster"
        api_family: "cluster.x-k8s.io"
        namespace_path: "metadata#namespace"
        name_path: "metadata#labels#cluster.x-k8s.io/cluster-name"
