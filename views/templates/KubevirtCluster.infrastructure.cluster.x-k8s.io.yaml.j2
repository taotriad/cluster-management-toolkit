---
kind: "KubevirtCluster"
api_family: "infrastructure.cluster.x-k8s.io"
default_command: "kubevirtclusters"
command:
  - "kubevirtclrs"
  - "kubevirtclr"
  - "kvclrs"
  - "kvclr"
listview:
  name: "Kubevirt Clusters"
  group: "Cluster Configuration"
  field_indexes:
    Wide:
      fields: ["namespace", "name", "controller", "cluster",
               "condition_ready", "age"]
      sortcolumn: "namespace"
    Normal:
      fields: ["namespace", "name", "cluster",
               "condition_ready", "age"]
      sortcolumn: "namespace"
  fields:
    cluster:
      header: "Cluster:"
      path: "metadata#labels#cluster.x-k8s.io/cluster-name"
      type: "str"
    {%- for line in field_condition_ready.split("\n") %}
    {{ line }}
    {%- endfor %}
    {%- for line in field_controller.split("\n") %}
    {{ line }}
    {%- endfor %}
infoview:
  name: "Kubevirt Cluster Info"
  infopad:
    row_indexes:
      Normal:
        fields: ["cluster", "controller", "control_plane_endpoint",
                 "ssh_keys", "condition_ready"]
    rows:
      cluster:
        header: [["Cluster:", ["main", "infoheader"]]]
        path: "metadata#labels#cluster.x-k8s.io/cluster-name"
        type: "str"
      {%- for line in row_condition_ready.split("\n") %}
      {{ line }}
      {%- endfor %}
      {%- for line in row_controller.split("\n") %}
      {{ line }}
      {%- endfor %}
      control_plane_endpoint:
        header: [["Control Plane Endpoint:", ["main", "infoheader"]]]
        paths:
          - path: [["spec#controlPlaneEndpoint#host"],
                   ["spec#controlPlaneEndpoint#port"]]
            default: ["<unset>"]
            pathtype: "tuple"
            type: ["name", "name"]
        # FIXME: The formatter needs to be improved to handle lists
        # that contain addresses, etc.
        formatter: "list"
        formatting:
          field_colors:
            - type: "address"
            - type: "port"
          field_separators:
            - type: "port"
      ssh_keys:
        header: [["S", ["main", "infoheader_shortcut"]],
                 ["SH-Keys:", ["main", "infoheader"]]]
        path: "spec#sshKeys#dataSecretName"
        default: "<unset>"
        type: "str"
  shortcuts:
    {%- for line in shortcut_controller.split("\n") %}
    {{ line }}
    {%- endfor %}
    "Open SSH key data secret":
      key: "s"
      modifier: "shift"
      read_only: true
      helptext: "Open info page for SSH key Secret"
      action: "call"
      action_call: "resourceinfodispatch_with_lookup"
      action_args:
        _pass_obj: true
        kind: "Secret"
        namespace_path: "metadata#namespace"
        name_path: "spec#sshKeys#dataSecretName"
    "View Control Plane Service Template":
      key: "t"
      modifier: "shift"
      read_only: true
      helptext: "Show Control Plane Service Template"
      action: "call"
      action_call: "view_yaml"
      action_args:
        title: "Control Plane Service Template"
        _pass_obj: true
        path: "spec#controlPlaneServiceTemplate"
